name: Manual Build DEB
on:
  workflow_dispatch:

jobs:
  build:
    name: Build YTLite DEB
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: v5.2b4
          submodules: recursive

      - name: Install Dependencies
        run: brew install make ldid dpkg

      - name: Setup Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git ~/theos
          echo "THEOS=~/theos" >> $GITHUB_ENV
          echo "~/theos/bin" >> $GITHUB_PATH

      - name: Download iOS SDK
        run: |
          git clone --quiet -n --depth=1 --filter=tree:0 https://github.com/theos/sdks/
          cd sdks
          git sparse-checkout set --no-cone iPhoneOS16.5.sdk
          git checkout
          mv *.sdk ~/theos/sdks

      - name: Clone Header Dependencies
        run: |
          cd ..
          git clone --quiet --depth=1 https://github.com/PoomSmart/YouTubeHeader.git
          git clone --quiet --depth=1 https://github.com/PoomSmart/PSHeader.git
          git clone --quiet --depth=1 https://github.com/PoomSmart/YTVideoOverlay.git
          ln -s YouTubeHeader YTHeaders 2>/dev/null || true

      - name: Download Roothide Header
        run: |
          curl -Lo ~/theos/include/roothide.h https://raw.githubusercontent.com/roothide/libroothide/main/roothide.h

      - name: Build Tweak
        run: |
          ls -R ../YouTubeHeader | head -n 20
          make clean package FINALPACKAGE=1
        env:
          THEOS: ~/theos

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ytlite-free-deb
          path: packages/*.deb
